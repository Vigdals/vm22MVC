@using Microsoft.EntityFrameworkCore
@model TournamentModel
<h3>Poengoversikt</h3>
@foreach (var user in Model.TippeModels.Select(t => t.userName).Distinct())
{
    <p><text>@user - @Model.CalculateScoreByUserName(user)p</text></p>
}

@*Trying to sort the table before presenting it*@
@functions
{
    public string SortTable(TournamentModel tournament)
    {

    }
}
